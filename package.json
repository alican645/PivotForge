{
  "name": "pivotforge-browser-assets",
  "private": true,
  "description": "Test harness for the PivotForge.AspNetCore browser assets. Not published; the assets ship inside the NuGet package.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
